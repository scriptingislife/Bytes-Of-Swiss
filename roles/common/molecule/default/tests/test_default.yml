---
package:
  redis:
    installed: true

port:
  tcp:6379:
    listening: true

command:
  redis-cli ping:
    exit-status: 0
    stdout:
      - PONG
    stderr: []
    timeout: 1000
